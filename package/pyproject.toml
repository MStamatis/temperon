[project]
name = "quench-opt"
version = "0.1.0.dev0"
description = "Quench: pay for sharpness-aware minimization only where it pays you back"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Stamatis Mastromichalakis", email = "stamatis@tmnetworks.gr" }]
keywords = ["pytorch", "optimizer", "sharpness-aware-minimization", "sam", "muon", "training-efficiency"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = ["torch>=2.0"]

[project.urls]
Homepage = "https://github.com/MStamatis/quench"
Repository = "https://github.com/MStamatis/quench"
Issues = "https://github.com/MStamatis/quench/issues"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/quench_opt"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
